extends layout 

block content
    section.section-white
        h1.fw-bold
            i.fas.fa-users.me-2
            | Barátok
        hr

        if friends 
            for friend in friends 
                a(href=makeUserUrl(friend))= friend.name
                br

        else 
            p(style="font-style: italic;").text-muted A barátaid litáját itt találod majd, miután visszajelöl egy pár ember! ;)
